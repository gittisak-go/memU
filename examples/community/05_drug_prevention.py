"""
ตัวอย่างที่ 5: เครื่องมือป้องกันยาเสพติดในชุมชน
ความเป็นส่วนตัวเป็นอันดับแรก — ใช้รหัสแทนชื่อจริงเสมอ

วิธีใช้:
    pip install memu-py
    export OPENAI_API_KEY=your_api_key
    python examples/community/05_drug_prevention.py
"""

import asyncio
import os
import sys
import tempfile

sys.path.insert(0, os.path.abspath("src"))
from memu.app import MemoryService

# คำเตือนความเป็นส่วนตัว — แสดงก่อนเสมอ
คำเตือน = """
╔══════════════════════════════════════════════════════════════╗
║           ⚠️  คำเตือนด้านความเป็นส่วนตัว  ⚠️                ║
║  ✗ ห้ามบันทึกชื่อจริง นามสกุล หรือข้อมูลส่วนตัวใดๆ          ║
║  ✗ ห้ามระบุที่อยู่หรือสถานที่จริงของบุคคล                    ║
║  ✓ ใช้รหัสสมมติแทนชื่อเสมอ (เช่น P001, Y001)               ║
║  ✓ บันทึกเฉพาะกิจกรรมชุมชนและสถิติ                          ║
║  ✓ ปฏิบัติตาม PDPA พ.ศ. 2562                               ║
║  🆘 สายด่วนยาเสพติด: 1300 (24 ชั่วโมง ไม่เปิดเผยตัวตน)      ║
╚══════════════════════════════════════════════════════════════╝
"""

# ข้อมูลกิจกรรมชุมชน — ใช้รหัสแทนชื่อทุกอย่าง (สมมติ)
ข้อมูลกิจกรรม = (
    "รายงานกิจกรรมป้องกันยาเสพติด Q1/2025\n"
    "1. อบรมผู้นำชุมชน (15 ก.พ.) ผู้เข้าร่วม L001-L035 จำนวน 35 คน "
    "ผลลัพธ์: เข้าใจสัญญาณเตือนภัย ความพึงพอใจ 4.2/5\n"
    "2. โครงการเยาวชนสร้างสรรค์ (ทุกเสาร์) กลุ่ม Y001-Y048 จำนวน 48 คน "
    "กิจกรรม: ดนตรี กีฬา ศิลปะ เข้าร่วมสม่ำเสมอ 85%\n"
    "3. เยี่ยมบ้านเชิงป้องกัน (ก.พ.) H001-H020 จำนวน 20 ครัวเรือน "
    "พบปัจจัยเสี่ยง H007 H013 H019 ส่งต่อบริการ 2 ราย\n"
    "ผลลัพธ์: ขอรับความช่วยเหลือสมัครใจ C001-C005 ส่งบำบัดสำเร็จ 4 ราย"
)

# ข้อมูลทรัพยากรในพื้นที่ (สมมติ)
ข้อมูลทรัพยากร = (
    "ทรัพยากรในพื้นที่\n"
    "ผู้เชี่ยวชาญ: นักสังคมสงเคราะห์ 2 คน (ผ่านสำนักพัฒนาสังคม) "
    "นักจิตวิทยา 1 คน (ทุกวันพุธ) แพทย์เวชปฏิบัติ 1 คน (รพ.ชุมชน)\n"
    "ศูนย์บำบัด: ศูนย์จังหวัด (รับผู้ป่วยนอก) สถานพักฟื้น 90 วัน 20 คน (เต็มแล้ว)\n"
    "สายด่วน: ยาเสพติด 1300 สุขภาพจิต 1323 เด็กเยาวชน 1387"
)


async def บันทึกข้อมูล(service: MemoryService) -> None:
    """บันทึกข้อมูลกิจกรรมและทรัพยากร (ไม่มีชื่อจริง)"""
    print("\n📋 บันทึกข้อมูลกิจกรรมชุมชน...")
    for ชื่อ, เนื้อหา in [("กิจกรรม", ข้อมูลกิจกรรม), ("ทรัพยากร", ข้อมูลทรัพยากร)]:
        with tempfile.NamedTemporaryFile(mode="w", suffix=".txt", delete=False, encoding="utf-8") as f:
            f.write(เนื้อหา)
            path = f.name
        try:
            result = await service.memorize(resource_url=path, modality="document",
                                            where={"user_id": "ชุมชนป้องกันยาเสพติด"})
            print(f"  ✓ {ชื่อ} — {len(result.get('items', []))} รายการ")
        finally:
            os.unlink(path)


async def สืบค้น(service: MemoryService) -> None:
    """สาธิตการสืบค้นกิจกรรมและทรัพยากร"""
    print("\n🔍 สืบค้นข้อมูล...")
    where = {"user_id": "ชุมชนป้องกันยาเสพติด"}
    คำถาม = [
        "กิจกรรมป้องกันยาเสพติดที่ดำเนินการมีอะไรบ้าง? ผลลัพธ์เป็นอย่างไร?",
        "มีผู้เชี่ยวชาญและศูนย์บำบัดอะไรบ้าง? ติดต่อได้อย่างไร?",
    ]
    for ถาม in คำถาม:
        print(f"\n❓ {ถาม}")
        ผล = await service.retrieve(queries=[{"role": "user", "content": ถาม}], where=where)
        for i, item in enumerate(ผล.get("items", [])[:2], 1):
            print(f"  {i}. {item.get('summary', '')[:110]}")
        if not ผล.get("items"):
            print("  ⚠️  ไม่พบข้อมูล")


async def หลัก() -> None:
    """ฟังก์ชันหลัก — เครื่องมือป้องกันยาเสพติดในชุมชน"""
    # แสดงคำเตือนความเป็นส่วนตัวก่อนเสมอ
    print(คำเตือน)
    input("กด Enter เพื่อดำเนินการต่อ (Ctrl+C เพื่อยกเลิก): ")

    print("=" * 55)
    print("🛡️  ระบบสนับสนุนป้องกันยาเสพติด (รหัสแทนชื่อจริง)")
    print("=" * 55)
    api_key = os.getenv("OPENAI_API_KEY")
    if not api_key:
        print("❌ กรุณาตั้งค่า OPENAI_API_KEY ก่อนใช้งาน")
        return
    service = MemoryService(
        llm_profiles={"default": {"api_key": api_key, "chat_model": "gpt-4o-mini"}},
        memorize_config={"memory_categories": [
            {"name": "กิจกรรม", "description": "กิจกรรมป้องกันและผลลัพธ์เชิงสถิติ"},
            {"name": "ทรัพยากร", "description": "ผู้เชี่ยวชาญ ศูนย์บำบัด และสายด่วน"},
        ]},
    )
    print("  ✓ เริ่มต้นระบบสำเร็จ")
    await บันทึกข้อมูล(service)
    await สืบค้น(service)
    print("\n✅ การสาธิตเสร็จสมบูรณ์")
    print("🔒 ข้อมูลส่วนตัวได้รับการคุ้มครองตลอดการใช้งาน")


if __name__ == "__main__":
    asyncio.run(หลัก())
