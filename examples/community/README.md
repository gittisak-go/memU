# memU เพื่อชุมชนไทย 🇹🇭

ชุดตัวอย่างนี้แสดงให้เห็นว่า **memU** สามารถช่วยงานชุมชนในประเทศไทยได้อย่างไร โดยใช้ AI ที่มีความจำเพื่อแก้ปัญหาจริงในชีวิตประจำวัน

---

## ทำไม AI ถึงต้องการความจำสำหรับงานชุมชน?

AI ทั่วไปจะ "ลืม" ทุกอย่างหลังการสนทนาจบลง ซึ่งไม่เหมาะสำหรับงานชุมชนที่ต้องการความต่อเนื่อง เช่น:

- **ครู** ต้องจำข้อมูลนักเรียนหลายสิบคนตลอดปีการศึกษา
- **อาสาสมัคร** ต้องจำข้อมูลสุขภาพของผู้สูงอายุที่ดูแลอยู่
- **คณะกรรมการหมู่บ้าน** ต้องติดตามการเงินและกิจกรรมต่อเนื่องหลายเดือน

memU แก้ปัญหานี้ด้วยระบบความจำถาวรที่สืบค้นได้ด้วยภาษาธรรมชาติ

---

## ตัวอย่างทั้ง 5 กรณีการใช้งาน

| ไฟล์ | กลุ่มเป้าหมาย | ปัญหาที่แก้ | วิธีใช้งาน |
|------|---------------|-------------|------------|
| `01_teacher_student.py` | ครูและนักเรียน | จำจุดแข็ง-อ่อน คะแนน และพฤติกรรมนักเรียนรายบุคคล | บันทึกโปรไฟล์นักเรียน แล้วถามว่า "น้องมินมีปัญหาด้านไหน?" |
| `02_elderly_care.py` | ผู้ดูแลผู้สูงอายุ | จำรายการยา นัดหมอ และอาการผิดปกติ | บันทึกข้อมูลสุขภาพ แล้วถามว่า "คุณตาต้องทานยาอะไรบ้าง?" |
| `03_family_assistant.py` | ทุกครอบครัว | จำตาราง วันเกิด ความชอบ และรายการที่ต้องทำ | บันทึกข้อมูลแต่ละคน แล้วถามว่า "ใครยุ่งสัปดาห์นี้?" |
| `04_community_economic.py` | กลุ่มออมทรัพย์/OTOP | ติดตามรายรับรายจ่าย หุ้นสมาชิก และสินค้า | บันทึกข้อมูลการเงิน แล้วถามว่า "กำไรเดือนนี้เท่าไร?" |
| `05_drug_prevention.py` | อาสาสมัครป้องกันยาเสพติด | บันทึกกิจกรรมชุมชนและทรัพยากรอย่างปลอดภัย | ใช้รหัสแทนชื่อจริงเสมอ มีคำเตือนความเป็นส่วนตัว |

---

## การติดตั้ง (3 คำสั่ง)

```bash
# 1. ติดตั้ง memU
pip install memu-py

# 2. ตั้งค่า API Key
export OPENAI_API_KEY=your_api_key_here

# 3. รันตัวอย่าง (เปลี่ยนเลขตามต้องการ)
python examples/community/01_teacher_student.py
```

---

## วิธีปรับใช้กับบริบทของคุณเอง

### 1. เปลี่ยนข้อมูลสมมติ
แก้ไขตัวแปรข้อมูลในแต่ละไฟล์ให้ตรงกับข้อมูลจริงของคุณ

### 2. เพิ่มหมวดหมู่ความจำ
```python
service = MemoryService(
    llm_profiles={"default": {"api_key": api_key, "chat_model": "gpt-4o-mini"}},
    memorize_config={
        "memory_categories": [
            {"name": "หมวดหมู่ของคุณ", "description": "คำอธิบายสิ่งที่ต้องจำ"},
        ]
    },
)
```

### 3. แยก user_id ตามความต้องการ
- ใช้ `where={"user_id": "ชื่อคน"}` เพื่อแยกข้อมูลรายบุคคล
- ใช้ `where={"user_id": "กลุ่ม"}` เพื่อเก็บข้อมูลรวมของกลุ่ม

### 4. ปรับภาษาในการสืบค้น
```python
# ถามเป็นภาษาไทยได้เลย
result = await service.retrieve(
    queries=[{"role": "user", "content": "คำถามของคุณเป็นภาษาไทย"}]
)
```

---

## แนวทางการเชื่อมต่อกับ LINE และ N8N

### เชื่อมกับ LINE Official Account
1. ใช้ LINE Messaging API รับข้อความจากผู้ใช้
2. ส่งข้อความมาประมวลผลด้วย memU
3. ส่งคำตอบกลับผ่าน LINE Reply API

```python
# ตัวอย่างโครงสร้างการเชื่อมต่อ LINE
async def handle_line_message(user_id: str, message: str):
    result = await service.retrieve(
        queries=[{"role": "user", "content": message}],
        where={"user_id": user_id},
    )
    return result.get("items", [])
```

### เชื่อมกับ N8N Workflow
1. สร้าง HTTP Request Node ใน N8N ชี้มาที่ API ของคุณ
2. ใช้ Function Node ประมวลผลด้วย memU
3. ส่งผลลัพธ์ไปยัง Node ถัดไป (LINE, Email, Google Sheets)

---

## ข้อความปิดท้าย

> "เทคโนโลยี AI ไม่ควรอยู่แค่ในมือของนักพัฒนาหรือบริษัทใหญ่ แต่ควรเข้าถึงได้โดยทุกคน ไม่ว่าจะเป็นครูในโรงเรียนชนบท อาสาสมัครดูแลผู้สูงอายุ หรือกลุ่มออมทรัพย์ในหมู่บ้าน"

memU เพื่อชุมชนไทย ถูกสร้างด้วยความเชื่อว่า **AI ที่มีความจำ = เครื่องมือที่ใช้งานได้จริง** 🤝

---

*ตัวอย่างทั้งหมดใช้ข้อมูลสมมติ ไม่ใช่ข้อมูลบุคคลจริง*
