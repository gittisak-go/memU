# memU เพื่อชุมชนไทย 🇹🇭

คู่มือการใช้ memU เป็น memory engine สำหรับชุมชนไทย — ใช้ Python ง่าย ๆ ได้ทันที

---

## ทำไม AI ถึงต้องมีความจำสำหรับงานชุมชน?

AI ทั่วไปจะ "ลืม" ทุกอย่างหลังจบการสนทนา แต่งานชุมชนต้องการความต่อเนื่อง:
- ครูต้องจำว่านักเรียนแต่ละคนติดเรื่องอะไร
- ลูกหลานต้องรู้ว่าผู้สูงอายุนัดหมอเมื่อไร
- กลุ่มออมทรัพย์ต้องรู้ว่าใครยังไม่จ่ายเงิน

**memU แก้ปัญหานี้** โดยเป็น memory engine ที่ AI จดจำข้อมูลสำคัญได้ข้ามเซสชัน

---

## 5 Use Cases สำหรับชุมชนไทย

| ไฟล์ | กลุ่มเป้าหมาย | ปัญหาที่แก้ | วิธีใช้ |
|------|--------------|------------|--------|
| `01_teacher_student.py` | ครู / นักเรียน | ครูจำข้อมูลนักเรียนไม่ครบ | บันทึกจุดอ่อน-จุดแข็ง ถามได้ทันที |
| `02_elderly_care.py` | ผู้สูงอายุ / ลูกหลาน | จำยา นัดหมอ ไม่ได้ | บันทึกยา-นัดหมอ ส่ง LINE reminder |
| `03_family_assistant.py` | ครอบครัว | ตารางแต่ละคนวุ่นวาย | บันทึกตารางแยก user_id ต่อคน |
| `04_community_economic.py` | กลุ่มวิสาหกิจ / ออมทรัพย์ | บัญชีกระจัดกระจาย | บันทึกรายรับ-รายจ่าย สต็อก สมาชิก |
| `05_drug_prevention.py` | อาสาสมัคร / ชุมชน | ข้อมูลกิจกรรมหาย | บันทึกกิจกรรม ทรัพยากร (ใช้รหัส) |

---

## ขั้นตอนติดตั้ง (3 คำสั่ง)

```bash
# 1. ติดตั้ง memU
pip install memu-py

# 2. ตั้งค่า API key
export OPENAI_API_KEY=your_api_key_here

# 3. รัน example
python examples/community/01_teacher_student.py
```

---

## วิธีปรับใช้กับบริบทของตัวเอง

### เปลี่ยนหมวดความจำ

```python
หมวดความจำ = [
    {"name": "ชื่อหมวด", "description": "คำอธิบายว่าเก็บอะไร"},
    # เพิ่มหมวดตามต้องการ
]
service = MemUService(
    llm_profiles={"default": {"api_key": api_key, "chat_model": "gpt-4o-mini"}},
    memorize_config={"memory_categories": หมวดความจำ},
)
```

### แยกข้อมูลตาม user_id

```python
# บันทึก
await service.memorize(resource_url="ไฟล์.txt", modality="document",
                        where={"user_id": "user_001"})

# ดึงข้อมูลเฉพาะคน
result = await service.retrieve(
    queries=[{"role": "user", "content": "คำถาม"}],
    where={"user_id": "user_001"}
)
```

### เปลี่ยนแหล่งข้อมูล

```python
# รองรับหลายประเภท
await service.memorize(resource_url="file.txt", modality="document")
await service.memorize(resource_url="chat.json", modality="conversation")
await service.memorize(resource_url="photo.png", modality="image")
```

---

## แนวทางต่อยอดกับ LINE / N8N

### LINE Messaging API (ตัวอย่าง pseudocode)

```python
import httpx

async def ส่ง_LINE(ข้อความ: str, user_id: str, token: str):
    headers = {"Authorization": f"Bearer {token}"}
    body = {
        "to": user_id,
        "messages": [{"type": "text", "text": ข้อความ}]
    }
    async with httpx.AsyncClient() as client:
        await client.post(
            "https://api.line.me/v2/bot/message/push",
            headers=headers, json=body
        )
```

### N8N Integration

1. ใช้ **HTTP Request node** เรียก API ของ memU
2. ใช้ **Schedule Trigger** สำหรับ reminder อัตโนมัติ
3. ใช้ **Webhook** รับข้อความจาก LINE แล้วส่งไป memU

---

## ข้อมูลติดต่อชุมชน

- **GitHub Issues**: เปิด issue ที่ [github.com/gittisak-go/memU](https://github.com/gittisak-go/memU)
- **ขอความช่วยเหลือ**: อธิบายปัญหาใน issue พร้อมข้อมูล OS และ Python version
- **ร่วมพัฒนา**: ดู [CONTRIBUTING.md](../../CONTRIBUTING.md)

---

## หมายเหตุด้านความเป็นส่วนตัว

- **`05_drug_prevention.py`**: ใช้รหัสแทนชื่อเสมอ ห้ามบันทึกข้อมูลที่ระบุตัวบุคคลได้
- **ข้อมูลเด็ก**: ควรได้รับความยินยอมจากผู้ปกครองก่อนบันทึก
- **ผู้สูงอายุ**: ข้อมูลสุขภาพเป็นข้อมูลส่วนบุคคลชั้นพิเศษ ควรระมัดระวัง
- **ทุก example**: ข้อมูลตัวอย่างเป็นข้อมูลสมมติ ไม่ใช่ข้อมูลจริง

---

> **AI เพื่อสังคม** — memU ถูกออกแบบให้ทุกคนเข้าถึงได้ ไม่ใช่แค่นักพัฒนา
> ชุมชนไทยสามารถใช้ AI ที่มีความจำเพื่อแก้ปัญหาจริงในชีวิตประจำวัน
