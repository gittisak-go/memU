"""
ผู้ช่วยครอบครัว — จดจำทุกคนในบ้าน

ตัวอย่างนี้แสดงการใช้ memU ช่วยบริหารจัดการครอบครัว
บันทึกตารางเวลา วันเกิด งานสำคัญ และ to-do ของแต่ละคน

วิธีใช้:
    export OPENAI_API_KEY=your_api_key
    python examples/community/03_family_assistant.py
"""

import asyncio
import os

from memu.app import MemoryService as MemUService

# ข้อมูลสมมติของครอบครัว (ไม่ใช่ข้อมูลจริง)
สมาชิกครอบครัว = {
    "พ่อ_วิชัย": {
        "user_id": "dad_01",
        "ข้อมูล": """
ชื่อ: พ่อวิชัย อายุ 45 ปี
ตารางงาน: จันทร์-ศุกร์ 08:00-17:00 (ออฟฟิศ), เสาร์อาทิตย์หยุด
วันเกิด: 5 มิถุนายน
ความชอบ: ดูฟุตบอล ทำอาหาร
งานสำคัญ: ประชุมใหญ่บริษัท 20 มีนาคม, ส่งรายงาน 28 มีนาคม
""",
    },
    "แม่_สุดา": {
        "user_id": "mom_01",
        "ข้อมูล": """
ชื่อ: แม่สุดา อายุ 42 ปี
ตารางงาน: จันทร์-พุธ-ศุกร์ 09:00-15:00 (งานพาร์ทไทม์)
วันเกิด: 18 ธันวาคม
ความชอบ: ทำสวน อ่านหนังสือ
งานสำคัญ: พาลูกไปฉีดวัคซีน 12 มีนาคม, งานสังสรรค์เพื่อน 25 มีนาคม
""",
    },
    "ลูก_อาร์ม": {
        "user_id": "kid_01",
        "ข้อมูล": """
ชื่อ: น้องอาร์ม อายุ 14 ปี (มัธยม 2)
ตารางเรียน: จันทร์-ศุกร์ 07:30-16:00
กิจกรรมพิเศษ: ฟุตบอลอังคาร-พฤหัส 17:00-19:00, ติวพิเศษเสาร์ 09:00-12:00
วันเกิด: 30 เมษายน
สอบกลางภาค: 15-19 มีนาคม
""",
    },
}

# to-do ของครอบครัว
to_do_ครอบครัว = """
รายการสิ่งที่ต้องทำของครอบครัว:
1. ซื้อของขวัญสงกรานต์ก่อนวันที่ 10 เมษายน
2. จ่ายค่าเทอมน้องอาร์ม ภายใน 31 มีนาคม
3. นัดซ่อมแอร์ห้องนอน (เสียตั้งแต่เดือนที่แล้ว)
4. ต่ออายุประกันรถ ครบกำหนด 20 เมษายน
5. ทำความสะอาดบ้านใหญ่ก่อนสงกรานต์
"""


async def บันทึกสมาชิก(service: MemUService, ชื่อ: str, ข้อมูล: dict):
    """บันทึกข้อมูลสมาชิกแต่ละคน"""
    ไฟล์ = f"/tmp/memu_family/{ชื่อ}.txt"
    with open(ไฟล์, "w", encoding="utf-8") as f:
        f.write(ข้อมูล["ข้อมูล"])
    try:
        await service.memorize(
            resource_url=ไฟล์,
            modality="document",
            where={"user_id": ข้อมูล["user_id"]},
        )
        print(f"  ✓ บันทึกข้อมูล {ชื่อ} เรียบร้อย")
    except Exception as e:
        print(f"  ✗ บันทึก {ชื่อ} ไม่สำเร็จ: {e}")


async def main():
    print("ผู้ช่วยครอบครัว — จดจำทุกคนในบ้าน")
    print("-" * 50)

    api_key = os.getenv("OPENAI_API_KEY")
    if not api_key:
        raise ValueError("กรุณาตั้งค่า OPENAI_API_KEY")

    หมวดความจำ = [
        {"name": "ตารางเวลา", "description": "ตารางงาน ตารางเรียน และกิจกรรมของสมาชิก"},
        {"name": "วันเกิดและงานสำคัญ", "description": "วันเกิด งานสำคัญ และเหตุการณ์พิเศษ"},
        {"name": "ความชอบ", "description": "งานอดิเรก ของที่ชอบ และความสนใจ"},
        {"name": "todo_ครอบครัว", "description": "รายการสิ่งที่ต้องทำของครอบครัว"},
    ]

    service = MemUService(
        llm_profiles={"default": {"api_key": api_key, "chat_model": "gpt-4o-mini"}},
        memorize_config={"memory_categories": หมวดความจำ},
    )

    os.makedirs("/tmp/memu_family", exist_ok=True)

    # บันทึกข้อมูลสมาชิกแต่ละคน (แยกตาม user_id)
    print("\n👨‍👩‍👦 กำลังบันทึกข้อมูลสมาชิกครอบครัว...")
    for ชื่อ, ข้อมูล in สมาชิกครอบครัว.items():
        await บันทึกสมาชิก(service, ชื่อ, ข้อมูล)

    # บันทึก to-do ของครอบครัว
    ไฟล์_todo = "/tmp/memu_family/todo.txt"
    with open(ไฟล์_todo, "w", encoding="utf-8") as f:
        f.write(to_do_ครอบครัว)
    try:
        await service.memorize(resource_url=ไฟล์_todo, modality="document")
        print("  ✓ บันทึก to-do ครอบครัว เรียบร้อย")
    except Exception as e:
        print(f"  ✗ บันทึก to-do ไม่สำเร็จ: {e}")

    # ถามความจำ
    print("\n🔍 ถามผู้ช่วยครอบครัว...")
    คำถาม = [
        "อาทิตย์นี้ใครยุ่งบ้าง?",
        "วันเกิดใครใกล้ถึงแล้ว?",
        "มีอะไรที่ต้องทำก่อนสงกรานต์บ้าง?",
        "น้องอาร์มสอบเมื่อไร?",
    ]

    for คำถามข้อ in คำถาม:
        print(f"\n❓ ถามว่า: {คำถามข้อ}")
        try:
            ผลลัพธ์ = await service.retrieve(queries=[{"role": "user", "content": คำถามข้อ}])
            คำตอบ = ผลลัพธ์.get("answer", "ไม่พบข้อมูล")
            print(f"🤖 ตอบ: {คำตอบ}")
        except Exception as e:
            print(f"  ✗ ไม่สามารถดึงข้อมูลได้: {e}")

    print("\n✅ เสร็จสิ้น — ผู้ช่วยครอบครัวพร้อมใช้งาน")


if __name__ == "__main__":
    asyncio.run(main())
