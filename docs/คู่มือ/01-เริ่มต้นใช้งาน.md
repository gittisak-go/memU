[🏠 หน้าหลัก](../../README.md) | [📚 สารบัญ](README.md)

---

# บทที่ 1: เริ่มต้นใช้งาน memU

## memU คืออะไร?

memU คือ framework ด้านหน่วยความจำสำหรับ **AI agents ที่ทำงานเชิงรุกตลอด 24/7** ออกแบบมาเพื่อแก้ปัญหาที่ว่า — AI ขาดหน่วยความจำระยะยาวที่จัดระเบียบได้

### แนวคิด "หน่วยความจำเหมือน File System"

memU ใช้แนวคิดเปรียบหน่วยความจำเหมือน file system:

```
memory/
├── preferences/           ← Category (เหมือน folder)
│   ├── communication_style.md   ← Memory Item (เหมือน file)
│   └── topic_interests.md
├── relationships/
│   ├── contacts/
│   └── interaction_history/
├── knowledge/
│   ├── domain_expertise/
│   └── learned_skills/
└── context/
    ├── recent_conversations/
    └── pending_tasks/
```

เหมือน file system ที่เปลี่ยน bytes ดิบๆ ให้เป็นข้อมูลที่มีระเบียบ memU แปลง interactions ดิบๆ ให้เป็น **ข้อมูลอัจฉริยะที่มีโครงสร้าง ค้นหาได้ และทำงานเชิงรุก**

### ความแตกต่างจาก memory systems อื่น

| คุณสมบัติ | memU | ระบบทั่วไป |
|---------|------|-----------|
| Proactive | ✅ ทำงานเองเชิงรุก | ❌ รอคำสั่งเท่านั้น |
| Hierarchical | ✅ 3 ชั้น (Resource → Item → Category) | ❌ แบบ flat |
| Real-time | ✅ ประมวลผลทันที | ⏱ อาจมี delay |
| Cost Efficient | ✅ ลด token cost | ❌ ต้นทุนสูง |

---

## ข้อกำหนดเบื้องต้น

ก่อนเริ่มต้นใช้งาน memU คุณต้องมี:

1. **Python 3.13+**
   ```bash
   python --version  # ต้องเป็น 3.13 ขึ้นไป
   ```

2. **OpenAI API key** (หรือ provider ที่รองรับ)
   - สมัครที่ [platform.openai.com](https://platform.openai.com)
   - หรือใช้ OpenRouter, Qwen, หรือ provider อื่น (ดูบทที่ 6)

3. **Git** (สำหรับติดตั้งจาก source)

---

## การติดตั้ง

### วิธีที่ 1: ติดตั้งด้วย pip

```bash
pip install memu-py
```

### วิธีที่ 2: ติดตั้งด้วย uv (แนะนำ)

[uv](https://github.com/astral-sh/uv) คือ Python package manager ที่เร็วกว่า pip

```bash
# ติดตั้ง uv ก่อน (ถ้ายังไม่มี)
curl -LsSf https://astral.sh/uv/install.sh | sh

# ติดตั้ง memU
uv add memu-py

# หรือในโปรเจกต์ใหม่
uv init my-project
cd my-project
uv add memu-py
```

### วิธีที่ 3: ติดตั้งจาก source (สำหรับ development)

```bash
# Clone repository
git clone https://github.com/NevaMind-AI/memU.git
cd memU

# ติดตั้ง development dependencies
make install

# หรือใช้ pip โดยตรง
pip install -e .
```

---

## การตั้งค่า Environment Variables

### OpenAI (ค่าเริ่มต้น)

```bash
# Linux/macOS
export OPENAI_API_KEY=sk-your-api-key-here

# Windows (Command Prompt)
set OPENAI_API_KEY=sk-your-api-key-here

# Windows (PowerShell)
$env:OPENAI_API_KEY="sk-your-api-key-here"
```

### OpenRouter

```bash
export OPENROUTER_API_KEY=your-openrouter-key
```

### ตั้งค่าถาวรใน `.env` file

```bash
# สร้างไฟล์ .env
echo "OPENAI_API_KEY=sk-your-key" >> .env

# โหลดใน Python
from dotenv import load_dotenv
load_dotenv()
```

---

## ทดสอบครั้งแรก: In-Memory

ทดสอบ memU ด้วย in-memory storage (ไม่ต้องติดตั้ง database):

```python
import asyncio
import os
from memu.app import MemoryService

async def main():
    # ตั้งค่า service ด้วย in-memory storage
    service = MemoryService(
        llm_profiles={
            "default": {
                "api_key": os.getenv("OPENAI_API_KEY"),
                "chat_model": "gpt-4o-mini",
            }
        },
        database_config={
            "metadata_store": {"provider": "inmemory"},
        },
    )

    print("✅ memU พร้อมใช้งาน!")

    # Memorize บทสนทนา
    result = await service.memorize(
        resource_url="examples/resources/conversations/conv1.json",
        modality="conversation",
        user={"user_id": "user_001"},
    )

    print(f"สกัด {len(result.get('items', []))} memory items")
    print(f"สร้าง {len(result.get('categories', []))} categories")

    # Retrieve หน่วยความจำ
    memories = await service.retrieve(
        queries=[
            {"role": "user", "content": {"text": "ความชอบของผู้ใช้คืออะไร?"}}
        ],
        where={"user_id": "user_001"},
        method="rag",
    )

    print("\n📚 หน่วยความจำที่พบ:")
    for item in memories.get("items", [])[:3]:
        print(f"  - {item.get('summary', '')[:80]}")

asyncio.run(main())
```

รันด้วย:
```bash
export OPENAI_API_KEY=your_api_key
python my_first_test.py
```

หรือใช้ตัวอย่างที่มาพร้อม repo:
```bash
export OPENAI_API_KEY=your_api_key
cd tests
python test_inmemory.py
```

---

## Troubleshooting ปัญหาที่พบบ่อย

### ❌ `ValueError: Please set OPENAI_API_KEY`

**สาเหตุ**: ไม่ได้ตั้งค่า API key

**แก้ไข**:
```bash
export OPENAI_API_KEY=sk-your-actual-key
```

ตรวจสอบว่าตั้งค่าแล้ว:
```bash
echo $OPENAI_API_KEY
```

---

### ❌ `ModuleNotFoundError: No module named 'memu'`

**สาเหตุ**: ยังไม่ได้ติดตั้ง memU

**แก้ไข**:
```bash
pip install memu-py
# หรือถ้าติดตั้งจาก source
pip install -e .
```

---

### ❌ `Python version error: requires Python 3.13+`

**สาเหตุ**: Python version เก่าเกินไป

**แก้ไข**:
```bash
# ติดตั้ง Python 3.13 ด้วย pyenv
pyenv install 3.13.0
pyenv local 3.13.0

# ตรวจสอบ version
python --version
```

---

### ❌ Connection timeout ไปยัง OpenAI

**สาเหตุ**: ปัญหาเครือข่ายหรือ API key ไม่ถูกต้อง

**แก้ไข**:
1. ตรวจสอบ API key ว่าถูกต้อง
2. ตรวจสอบ quota ที่ [platform.openai.com/usage](https://platform.openai.com/usage)
3. ลองใช้ OpenRouter แทน (ดูบทที่ 6)

---

### ❌ `asyncio.run()` error ใน Jupyter Notebook

**สาเหตุ**: Jupyter มี event loop อยู่แล้ว

**แก้ไข**:
```python
# ใช้ await โดยตรงใน Jupyter
result = await service.memorize(...)

# หรือใช้ nest_asyncio
import nest_asyncio
nest_asyncio.apply()
asyncio.run(main())
```

---

## ขั้นตอนถัดไป

- 📖 [บทที่ 2: แนวคิดหลัก](02-แนวคิดหลัก.md) — ทำความเข้าใจ 3 ชั้นหน่วยความจำ
- 📖 [บทที่ 3: API อ้างอิง](03-API-อ้างอิง.md) — รายละเอียด API ทั้งหมด
- 📖 [บทที่ 4: ตัวอย่างจริง](04-ตัวอย่างจริง.md) — โค้ดตัวอย่างพร้อมใช้
