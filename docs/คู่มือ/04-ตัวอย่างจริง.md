[🏠 หน้าหลัก](../../README.md) | [📚 สารบัญ](README.md)

---

# บทที่ 4: ตัวอย่างจริง

ตัวอย่างทั้งหมดในบทนี้อ้างอิงจากไฟล์ใน `examples/` ของ repo จริง

---

## ตัวอย่าง 1: ผู้ช่วย AI จดจำการสนทนา

**ไฟล์**: [`examples/example_1_conversation_memory.py`](../../examples/example_1_conversation_memory.py)

### คำอธิบาย

ตัวอย่างนี้แสดงให้เห็นวิธีประมวลผลไฟล์บทสนทนาหลายไฟล์และสร้าง memory categories โดยอัตโนมัติ ระบบจะ:

1. ประมวลผลไฟล์ JSON บทสนทนา 3 ไฟล์
2. สกัด memory items จากแต่ละบทสนทนา
3. จัดระเบียบ items เป็น categories
4. สร้างไฟล์ Markdown สำหรับแต่ละ category

### โค้ด

```python
import asyncio
import os
from memu.app import MemoryService


async def main():
    print("Example 1: Conversation Memory Processing")
    print("-" * 50)

    api_key = os.getenv("OPENAI_API_KEY")
    if not api_key:
        raise ValueError("Please set OPENAI_API_KEY environment variable")

    # สร้าง service ด้วย OpenAI
    service = MemoryService(
        llm_profiles={
            "default": {
                "api_key": api_key,
                "chat_model": "gpt-4o-mini",
            },
        },
    )

    # ไฟล์บทสนทนาที่จะประมวลผล
    conversation_files = [
        "examples/resources/conversations/conv1.json",
        "examples/resources/conversations/conv2.json",
        "examples/resources/conversations/conv3.json",
    ]

    # ประมวลผลทีละไฟล์
    total_items = 0
    categories = []

    for conv_file in conversation_files:
        if not os.path.exists(conv_file):
            continue

        result = await service.memorize(
            resource_url=conv_file,
            modality="conversation"
        )
        total_items += len(result.get("items", []))
        # categories อัปเดตหลังจาก memorize แต่ละครั้ง
        categories = result.get("categories", [])

    print(f"✓ ประมวลผล {len(conversation_files)} ไฟล์")
    print(f"✓ สกัด {total_items} memory items")
    print(f"✓ สร้าง {len(categories)} categories")

    # แสดงผล categories
    for cat in categories:
        print(f"\n📁 {cat['name']}")
        summary = cat.get('summary', '')[:100]
        print(f"   {summary}...")


asyncio.run(main())
```

### วิธีรัน

```bash
export OPENAI_API_KEY=your_api_key
python examples/example_1_conversation_memory.py
```

### ผลลัพธ์ที่คาดหวัง

```
Example 1: Conversation Memory Processing
--------------------------------------------------
✓ ประมวลผล 3 ไฟล์
✓ สกัด 15 memory items
✓ สร้าง 4 categories

📁 preferences
   ผู้ใช้ชอบ dark mode และ minimal UI ต้องการ notifications...

📁 technical_skills
   เชี่ยวชาญ Python, JavaScript มีประสบการณ์ใน React...

📁 work_context
   ทำงานใน startup ตำแหน่ง senior developer ทีม 5 คน...

📁 communication_style
   ชอบคำตอบสั้นกระชับ ใช้ bullet points...
```

Output files จะอยู่ที่ `examples/output/conversation_example/`

---

## ตัวอย่าง 2: Agent เรียนรู้และปรับปรุงตัวเอง

**ไฟล์**: [`examples/example_2_skill_extraction.py`](../../examples/example_2_skill_extraction.py)

### คำอธิบาย

ตัวอย่างนี้แสดง **Incremental Learning** — การเรียนรู้ทีละขั้นตอน:

1. ประมวลผล deployment logs ทีละไฟล์
2. แต่ละไฟล์ **อัปเดต** หน่วยความจำที่มีอยู่ (ไม่ได้แทนที่)
3. Category summaries วิวัฒนาการตาม knowledge ที่สะสม
4. สร้าง `skill.md` — คู่มือ deployment ที่เรียนจากประสบการณ์จริง

### Custom Memory Types และ Categories

```python
# กำหนด prompt พิเศษสำหรับ skill extraction
skill_prompt = """
You are analyzing an agent execution log. Extract:

1. **Action/Phase**: What was being attempted?
2. **Status**: SUCCESS ✅ or FAILURE ❌
3. **What Happened**: What was executed
4. **Outcome**: What worked/failed, metrics
5. **Root Cause** (for failures): Why did it fail?
6. **Lesson**: What did we learn?

Text: {resource}
"""

# กำหนด categories เอง
skill_categories = [
    {"name": "deployment_execution", "description": "Deployment actions"},
    {"name": "incident_response_rollback", "description": "Error handling"},
    {"name": "performance_monitoring", "description": "Metrics analysis"},
    {"name": "lessons_learned", "description": "Key insights"},
]

memorize_config = {
    "memory_types": ["skill"],
    "memory_type_prompts": {"skill": skill_prompt},
    "memory_categories": skill_categories,
}

service = MemoryService(
    llm_profiles={"default": {"api_key": api_key, "chat_model": "gpt-4o-mini"}},
    memorize_config=memorize_config,
)
```

### วิธีรัน

```bash
export OPENAI_API_KEY=your_api_key
python examples/example_2_skill_extraction.py
```

### ผลลัพธ์ที่คาดหวัง

```
Example 2: Incremental Skill Extraction
--------------------------------------------------
Processing files...

✓ ประมวลผล 3 ไฟล์
✓ สกัด 12 skills
✓ Output: examples/output/skill_example/
```

ไฟล์ output:
- `examples/output/skill_example/log_1.md` — หลังประมวลผล log แรก
- `examples/output/skill_example/log_2.md` — หลังประมวลผล log ที่สอง
- `examples/output/skill_example/log_3.md` — หลังประมวลผล log ที่สาม
- `examples/output/skill_example/skill.md` — **คู่มือสุดท้าย** จากทุก logs

---

## ตัวอย่าง 3: หน่วยความจำหลายรูปแบบ (Multimodal)

**ไฟล์**: [`examples/example_3_multimodal_memory.py`](../../examples/example_3_multimodal_memory.py)

### คำอธิบาย

ตัวอย่างนี้แสดงวิธีประมวลผลหลาย modalities (เอกสาร + ภาพ) และสร้าง unified memory categories:

1. ประมวลผล text documents 2 ไฟล์
2. ประมวลผล image 1 ไฟล์
3. รวม knowledge ข้าม modalities

### โค้ด

```python
import asyncio
import os
from memu.app import MemoryService


async def main():
    api_key = os.getenv("OPENAI_API_KEY")

    # กำหนด categories สำหรับ multimodal content
    multimodal_categories = [
        {"name": "technical_documentation", "description": "Technical guides"},
        {"name": "architecture_concepts", "description": "System architecture"},
        {"name": "best_practices", "description": "Best practices"},
        {"name": "code_examples", "description": "Code snippets"},
        {"name": "visual_diagrams", "description": "Visual concepts from images"},
    ]

    service = MemoryService(
        llm_profiles={
            "default": {
                "api_key": api_key,
                "chat_model": "gpt-4o-mini",  # ต้องรองรับ vision
            }
        },
        memorize_config={"memory_categories": multimodal_categories},
    )

    # ประมวลผลหลาย modalities
    resources = [
        ("examples/resources/docs/doc1.txt", "document"),
        ("examples/resources/docs/doc2.txt", "document"),
        ("examples/resources/images/image1.png", "image"),  # ← ภาพ!
    ]

    total_items = 0
    categories = []

    for resource_file, modality in resources:
        if not os.path.exists(resource_file):
            continue

        result = await service.memorize(
            resource_url=resource_file,
            modality=modality,
        )
        total_items += len(result.get("items", []))
        categories = result.get("categories", [])

    print(f"✓ ประมวลผล {len(resources)} resources ({2} documents + 1 image)")
    print(f"✓ สกัด {total_items} items จากทุก modalities")
    print(f"✓ รวม {len(categories)} unified categories")


asyncio.run(main())
```

### หมายเหตุสำคัญ

- ต้องใช้โมเดลที่รองรับ **vision** (เช่น `gpt-4o-mini`, `gpt-4o`)
- ภาพจะถูก analyze และสกัดเป็น text descriptions
- memory items จากภาพและเอกสารจะถูกรวมเป็น category เดียวกัน

### วิธีรัน

```bash
export OPENAI_API_KEY=your_api_key
python examples/example_3_multimodal_memory.py
```

---

## ตัวอย่าง 4: OpenRouter Integration

**ไฟล์**: [`examples/example_4_openrouter_memory.py`](../../examples/example_4_openrouter_memory.py)

### คำอธิบาย

ตัวอย่างนี้แสดงวิธีใช้ [OpenRouter](https://openrouter.ai) แทน OpenAI โดยตรง ประโยชน์ของ OpenRouter:

- เข้าถึง LLM หลายตัวผ่าน API เดียว
- ใช้ Claude, Gemini, Llama, และอื่นๆ
- API เดียวกับ OpenAI

### โค้ด

```python
import asyncio
import os
from memu.app import MemoryService


async def main():
    api_key = os.getenv("OPENROUTER_API_KEY")
    if not api_key:
        raise ValueError("Please set OPENROUTER_API_KEY environment variable")

    # ตั้งค่า OpenRouter
    service = MemoryService(
        llm_profiles={
            "default": {
                "provider": "openrouter",
                "client_backend": "httpx",
                "base_url": "https://openrouter.ai",
                "api_key": api_key,
                # เปลี่ยนโมเดลได้ตามต้องการ
                "chat_model": "anthropic/claude-3.5-sonnet",
                "embed_model": "openai/text-embedding-3-small",
            },
        },
    )

    conversation_files = [
        "examples/resources/conversations/conv1.json",
        "examples/resources/conversations/conv2.json",
        "examples/resources/conversations/conv3.json",
    ]

    total_items = 0
    categories = []

    for conv_file in conversation_files:
        if not os.path.exists(conv_file):
            print(f"ข้าม: {conv_file} ไม่พบ")
            continue

        print(f"กำลังประมวลผล: {conv_file}")
        result = await service.memorize(
            resource_url=conv_file,
            modality="conversation"
        )
        total_items += len(result.get("items", []))
        categories = result.get("categories", [])

    print(f"\nประมวลผล {len(conversation_files)} ไฟล์")
    print(f"สกัด {total_items} items")
    print(f"สร้าง {len(categories)} categories")


asyncio.run(main())
```

### วิธีรัน

```bash
export OPENROUTER_API_KEY=your_api_key
python examples/example_4_openrouter_memory.py
```

### โมเดลที่รองรับบน OpenRouter

| โมเดล | ชื่อใน OpenRouter | หมายเหตุ |
|------|-----------------|---------|
| Claude 3.5 Sonnet | `anthropic/claude-3.5-sonnet` | แนะนำสำหรับงาน complex |
| GPT-4o | `openai/gpt-4o` | รองรับ vision |
| Llama 3.1 | `meta-llama/llama-3.1-70b-instruct` | open source |
| Gemini Pro | `google/gemini-pro` | Google's model |

---

## เปรียบเทียบตัวอย่าง

| ตัวอย่าง | Use Case | Modality | Custom Config |
|---------|---------|---------|--------------|
| ตัวอย่าง 1 | Personal assistant | conversation | ❌ |
| ตัวอย่าง 2 | DevOps automation | document (logs) | ✅ custom types + categories |
| ตัวอย่าง 3 | Research assistant | document + image | ✅ custom categories |
| ตัวอย่าง 4 | Any + OpenRouter | conversation | ✅ custom provider |

---

## ขั้นตอนถัดไป

- 📖 [บทที่ 5: PostgreSQL](05-การติดตั้ง-PostgreSQL.md) — ย้ายไป production database
- 📖 [บทที่ 6: การผสานรวม](06-การผสานรวม.md) — เชื่อมต่อกับ LangGraph
